import { CinematicSplash } from '@/components/splash/CinematicSplash'

// Phase 1D: Cinematic splash with GSAP ScrollSmoother 4-layer parallax.
// SplashFallback is rendered when user prefers reduced motion.
export default function RootPage() {
  return <CinematicSplash />
}
